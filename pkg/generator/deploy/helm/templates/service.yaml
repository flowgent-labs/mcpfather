apiVersion: v1
kind: Service
metadata:
  name: {{ include "__MCP_SERVER_NAME__.fullname" . }}
  labels:
    {{- include "__MCP_SERVER_NAME__.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.config.mgmt.enabled }}
    - port: {{ .Values.service.mgmtPort }}
      targetPort: mgmt
      protocol: TCP
      name: mgmt
    {{- end }}
  selector:
    {{- include "__MCP_SERVER_NAME__.selectorLabels" . | nindent 4 }}
