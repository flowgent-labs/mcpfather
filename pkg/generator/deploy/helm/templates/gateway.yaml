{{- if (include "__MCP_SERVER_NAME__.useEnvoyGateway" .) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ include "__MCP_SERVER_NAME__.fullname" . }}
  labels:
    {{- include "__MCP_SERVER_NAME__.labels" . | nindent 4 }}
spec:
  gatewayClassName: {{ .Values.ingress.envoy.gatewayClassName }}
  listeners:
    {{- range .Values.ingress.envoy.hosts }}
    - name: {{ .host | replace "." "-" | replace "*" "wildcard" }}
      hostname: {{ .host | quote }}
      port: {{ $.Values.ingress.envoy.gatewayPort }}
      protocol: HTTP
      {{- if $.Values.ingress.envoy.tls }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ include "__MCP_SERVER_NAME__.fullname" $ }}-tls
      {{- end }}
    {{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "__MCP_SERVER_NAME__.fullname" . }}
  labels:
    {{- include "__MCP_SERVER_NAME__.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "__MCP_SERVER_NAME__.fullname" . }}
      port: {{ .Values.ingress.envoy.gatewayPort }}
  hostnames:
{{- range .Values.ingress.envoy.hosts }}
    - {{ .host | quote }}
{{- end }}
  rules:
    {{- range .Values.ingress.envoy.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: PathPrefix
            value: {{ .path | quote }}
      backendRefs:
        - name: {{ include "__MCP_SERVER_NAME__.fullname" $ }}
          port: {{ $.Values.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
